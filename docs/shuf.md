# `shuf` — Command Reference

**Source**: [linuxcommand.org](https://linuxcommand.org/lc3_man_pages/shuf1.php)
**Rust module**: `src-tauri/src/commands/shuf.rs`

**Status**: ✅ Fully Implemented (9/9 flags)

---

## ✅ Implemented Flags

- `-e`, `--echo`
  - treat each ARG as an input line

- `-i`, `--input-range`
  - treat each number LO through HI as an input line

- `-n`, `--head-count`
  - output at most COUNT lines

- `-o`, `--output`
  - write result to FILE instead of standard output

- `--random-source`
  - get random bytes from FILE

- `-r`, `--repeat`
  - output lines can be repeated

- `-z`, `--zero-terminated`
  - line delimiter is NUL, not newline

- `--help`
  - display this help and exit

- `--version`
  - output version information and exit

---

## ❌ Missing Flags (To Implement)

_All flags are implemented!_ 🎉
